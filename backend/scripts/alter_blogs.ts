import { Pool } from 'pg'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env' }); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); async function run() { try { await pool.query('ALTER TABLE blogs ADD COLUMN category VARCHAR(100) DEFAULT ''Mindfulness'', ADD COLUMN excerpt TEXT, ADD COLUMN reading_time VARCHAR(50) DEFAULT ''5 min read'';'); console.log('Successfully altered blogs table'); } catch(e) { console.error('Error altering table', e.message); } finally { pool.end(); } } run();
